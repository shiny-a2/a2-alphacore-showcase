# Private Research Design Boundary Update

A private, source-bound research design boundary was added for a future classifier family.

The public showcase records only the governance posture: no detector implementation, no signal generation, no market-data pipeline, no backtest, no broker path, no execution path, and no performance claim are included.
